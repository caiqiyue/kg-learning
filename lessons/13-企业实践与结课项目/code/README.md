# 代码说明

- `capstone_seed.cypher` 用于创建一个最小零售知识中台图谱。
- `capstone_eval.py` 用于展示知识中台不只评“能不能回答”，还要评抽取、引用、诊断命中、反馈修复周期和成本。

建议学员扩展：

1. 加入一次促销规则更新。
2. 加入一次商品改类和共享实体保护。
3. 加入一次问答助手与诊断助手的 GraphRAG 对比。
4. 加入一次 Agent 反馈回流和人工审核。
