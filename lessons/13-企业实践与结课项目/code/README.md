# 代码说明

- `capstone_seed.cypher` 用于创建一个最小销售诊断知识中台图谱。
- `capstone_eval.py` 用于展示知识中台不只评“能不能回答”，还要评抽取、引用、诊断命中、反馈修复周期和推荐采纳率。

建议学员扩展：

1. 加入一次诊断规则更新。
2. 加入一次指标口径变更和版本保护。
3. 加入一次问答助手与诊断推荐系统的 GraphRAG 对比。
4. 加入一次 Agent 反馈回流和人工审核。
